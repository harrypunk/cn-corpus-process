{
  "name": "chinese-peotry-process",
  "private": true,
  "type": "module",
  "scripts": {
    "ingest": "bun pipeline/ingest.ts",
    "lint": "oxlint src pipeline",
    "lint:fix": "oxlint --fix src pipeline",
    "format": "oxfmt src pipeline",
    "format:check": "oxfmt --check src pipeline",
    "typecheck": "tsc --noEmit",
    "check": "bun run lint && bun run format:check && bun run typecheck && bun test"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "drizzle-orm": "^0.45.2",
    "effect": "^3.22.1",
    "mysql2": "^3.24.2",
    "opencc-js": "^1.4.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
